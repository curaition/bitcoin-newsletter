{
  "$schema": "https://schema.railpack.com",
  "python": {
    "version": "3.11",
    "manager": "uv"
  },
  "build": {
    "commands": [
      "uv sync --frozen",
      "uv pip install --system uvicorn fastapi celery redis alembic psycopg2-binary python-multipart",
      "uv run alembic upgrade head"
    ]
  }
}
